name: Run Sample task that takes x minutes
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Sleep for 2 minutes
        run: sleep 120
